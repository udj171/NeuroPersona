# Web layer
Flask==2.3.2
Flask-SQLAlchemy==3.0.5
Flask-Cors==4.0.0
Werkzeug==2.3.6
Jinja2==3.1.2
MarkupSafe==2.1.1
itsdangerous==2.1.2
click==8.1.3
gunicorn==20.1.0

# Data layer
SQLAlchemy==2.0.19
psycopg2-binary==2.9.6
python-dotenv==1.0.0

# Inference. numpy alone runs the exported VAE weights; torch is only needed
# in the Kaggle training notebook, never in production.
numpy==1.24.3

# Tests
pytest==7.4.0

# Optional: local open-weights text generation (NARRATIVE_BACKEND=llm).
# Left commented so the default install stays small enough for free hosting.
# llama-cpp-python==0.3.2
